name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: "Thanks for submitting an issue to F1PyStats!\nWe will have a look at your issue, and get back to you ASAP :grin:"
        pr-message: "Thank you for submitting your first pull request :hug: \nIf you haven't seen our [Code of Conduct](), please be sure to check it out. You should also ensure your PR conforms to the pull request template provided, since this helps the maintainer review your contribution quicker."
